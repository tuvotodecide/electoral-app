appId: com.tuvotodecide
---
- runFlow: workflows/initialSetup.yaml

- tapOn:
    id: participateButtonRegular

#**---------------------------------------
#- tapOn: Obteniendo ubicación...
#**---------------------------------------
#- tapOn: Cargando recintos cercanos...
- scrollUntilVisible:
    element:
      id: ${ELECTORAL_RECORD_LOCATION}

- tapOn:
    id: ${ELECTORAL_RECORD_LOCATION}

- tapOn:
    id: ${ELECTORAL_RECORD_TABLE}

- runFlow:
    when:
        true: ${TEST_TYPE == 'fail'}
    file: components/voting/cases/wrongImageRecord.yaml
- runFlow:
    when:
        true: ${TEST_TYPE == 'pass'}
    file: components/voting/cases/validImageRecord.yaml
