appId: com.tuvotodecide
---
- runFlow: initialSetup.yaml

- tapOn:
    id: participateButtonRegular

#**---------------------------------------
#- tapOn: Obteniendo ubicación...
#**---------------------------------------
#- tapOn: Cargando recintos cercanos...
- scrollUntilVisible:
    element:
      id: ${ELECTORAL_RECORD_LOCATION}

- tapOn:
    id: ${ELECTORAL_RECORD_LOCATION}

- tapOn:
    id: ${ELECTORAL_RECORD_TABLE}

- runFlow:
    when:
        true: ${TEST_TYPE == 'fail'}
    file: wrongImageRecord.yaml
- runFlow:
    when:
        true: ${TEST_TYPE == 'pass'}
    file: validImageRecord.yaml
